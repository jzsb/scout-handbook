{
  "routes": [
    {
      "method": "GET",
      "path": "/bridgereps",
      "handler": "bridgerep.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bridgereps/count",
      "handler": "bridgerep.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bridgereps/:id",
      "handler": "bridgerep.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/bridgereps",
      "handler": "bridgerep.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/bridgereps/:id",
      "handler": "bridgerep.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/bridgereps/:id",
      "handler": "bridgerep.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
