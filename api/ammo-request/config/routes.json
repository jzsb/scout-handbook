{
  "routes": [
    {
      "method": "GET",
      "path": "/ammo-requests",
      "handler": "ammo-request.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ammo-requests/count",
      "handler": "ammo-request.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ammo-requests/:id",
      "handler": "ammo-request.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/ammo-requests",
      "handler": "ammo-request.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/ammo-requests/:id",
      "handler": "ammo-request.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/ammo-requests/:id",
      "handler": "ammo-request.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
